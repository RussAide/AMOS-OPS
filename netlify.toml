[build]
  command = "npm run build:client"
  publish = "dist/public"
  # Production publishes only after the CI-approved Railway identity is live.
  # This prevents the linked Git repository from racing the paired release job.
  ignore = "node ./scripts/netlify-git-build-policy.mjs"

[build.environment]
  NODE_VERSION = "24"

# Netlify serves the single browser build. VITE_AMOS_API_ORIGIN points this build
# to the existing Railway API; account profiles select Training or Operational.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
